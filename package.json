{
  "name": "barcode-scanner",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy public",
    "dev": "wrangler pages dev public"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}